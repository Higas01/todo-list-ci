name: 'Check Application'
on:
  pull_request:
    branches:
      - main
      - dev
jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Execute composite
        uses: ./.github/actions/action.yaml
      - name: 'Run Lint'
        run: pnpm run lint

  tests:
    runs-on: ubuntu-22.04
    steps:
      - name: Execute composite
        uses: ./.github/actions/action.yaml
      - name: 'Run tests'
        run: pnpm vitest
